{
  "name": "relayhq-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --port 44210 --host 0.0.0.0",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "prepare": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "test": "bun test"
  },
  "devDependencies": {
    "nuxt": "^3.13.2",
    "typescript": "^5.6.3"
  }
}
